@model IEnumerable<cf085115_Mis4200.Models.Appointment>

@{
    ViewBag.Title = "Appointment";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered table-hover table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Client.firstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Doctor.college)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.appointmentDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Client.firstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Doctor.college)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.appointmentDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.appointmentId }) <span class="glyphicon">&#x270f;</span>|
            @Html.ActionLink("Details", "Details", new { id=item.appointmentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.appointmentId })
        </td>
    </tr>
}

</table>
